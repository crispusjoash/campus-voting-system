# Frontend environment variables
# Copy this to .env and fill in the values before deploying

# The base URL of your backend API (no trailing slash)
# Local dev: leave blank (falls back to http://localhost:5000)
# Production: set to your deployed backend URL e.g. https://mmust-api.onrender.com
VITE_API_URL=
